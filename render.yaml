services:
  - type: web
    name: packlisten-converter
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: HEADLESS
        value: "1"
      - key: SECRET_KEY
        value: "change-me-in-production"
